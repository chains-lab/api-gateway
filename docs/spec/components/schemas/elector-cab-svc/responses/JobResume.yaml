type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - id
      - type
      - attributes
    properties:
      id:
        type: string
        description: User ID of the elector.
      type:
        type: string
        enum: [ job_resume ]
      attributes:
        $ref: "./JobResumeAttributes.yaml"